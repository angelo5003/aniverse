import { List } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const StyledDesktopNavBarContainer = styled(List.Root)`
  display: flex;
  flex-direction: row;
  align-items: center;
  list-style: none;
  width: 100%;
  justify-content: space-around;
`;
